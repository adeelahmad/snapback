package fidelity

// Fixtures returns the fixed fixture set handed to the resticfx generator.
func Fixtures() []Meta {
	panic("SUB-AGENT-TODO: T3 fixed set: >=6 regular files with pairwise-distinct mtimes (one sub-second like ...:07.123456789Z, one 1998-03-14T09:26:53Z); sizes 0, ~1 KiB and 3 MiB; modes 0644, 0600, 0755, 0444; names incl. \"with space.txt\", NFC \"café-日本.txt\", one file in a subdirectory; plus relative symlink link-to-small -> small.txt (LinkTarget set)")
}
