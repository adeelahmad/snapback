package config

// Redact returns a deep copy of c with environment values masked.
func Redact(c *Config) *Config {
	panic("SUB-AGENT-TODO: nil in nil out; deep copy c (slices and maps) and replace every environment value with \"***\", keeping keys")
}
