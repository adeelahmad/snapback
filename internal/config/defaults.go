package config

// DefaultPath returns the default config file path under the XDG config home.
func DefaultPath() (string, error) {
	panic("SUB-AGENT-TODO: absolute XDG_CONFIG_HOME/snapback/config.yaml, else HOME/.config/snapback/config.yaml, else error when HOME is empty")
}
