package cli

// LinkCommand returns the link command.
func LinkCommand(d Deps) Command {
	panic("SUB-AGENT-TODO: T2 - return Command{Name: \"link\"} whose Run makes PATH absolute against d.Getwd (never interpreting it), calls d.Linker.Ensure, and reports via WriteOK/WriteError; bad args are usage (exit 2)")
}
