---
type: memory
updated_at: 2026-09-22T01:49:18Z
last_retro_sprint: 1
---

# Entries

| id | applies_to | text | origin | recurrence | added |
|---|---|---|---|---|---|
| M-001 | all | Worktree isolation blocks writes to the shared-checkout STORY_DIR; copy init.md/output.md into the worktree, work there, and let the orchestrator copy output.md back after the gate passes. | sprint1 S1-01/T1 output.md; Plugin issues log | 2+ (every worktree task) | 2026-09-22 |
| M-002 | red-worker | Negative/"must-not-contain" tests pass vacuously on empty or missing input; assert the expected non-empty set/content exists before checking it lacks banned content. | sprint1 S1-05 (ownedFiles guard), S1-06/T2 (NoPackagePublishers/HonestyWords guards), S1-07/T6, S1-08 honesty scan | 4 | 2026-09-22 |
| M-003 | red-worker, structural-reviewer | Static "PASS-ON-RED" guard tests (compile guards, negative regression scans) are legitimate and must not be weakened just because they pass before GREEN exists; flag them for orchestrator adjudication instead of rewriting. | sprint1 S1-06/T1 TestGoreleaserVersionVarsCompile; S1-08/T2 TestReadmeClaimsNoWorkingRestore; S1-08/T4 | 3 | 2026-09-22 |
| M-004 | green-worker | golangci-lint scans test files too, not just production code; a lint issue left in a red-worker's test file can fail a later green-worker's full-matrix gate. | sprint1 S1-07/T5 (staticcheck QF1001 in workflow_test.go, required a lint-only RED retry) | 1 (high blast-radius; kept) | 2026-09-22 |
| M-005 | green-worker, planner | The whole-repo test matrix cannot pass mid-story while sibling tasks' scaffolded stubs still panic; run intermediate GREENs with GATE_RUN_MATRIX=0 plus a package-scoped matrix, and run the full matrix only on the last GREEN of a story. | sprint1 Plugin issues log; S1-01/T1 plan defect (0-test task can't pass matrix alone); repeated in every story's GREEN sequencing | 8 (every story) | 2026-09-22 |
| M-006 | planner, green-worker | Never pin a tool install with `@latest` in CI; pin an exact version so installs are reproducible and reviewable. | sprint1 ci.yml `go install …@latest` for goimports and govulncheck, found by S1-02+S1-04 structural review, fixed in S1-02/fix1 | 2 (two @latest installs in one file) | 2026-09-22 |
| M-007 | planner, green-worker | A config file for an external tool (e.g. goreleaser) must be validated by actually running that tool locally, not only by hand-written tests; an unverified schema field can pass every test and still break the real run. | sprint1 .goreleaser.yaml pinned v2.5.1 lacked `formats`; flagged as an untested risk at S1-06/T1 and T2, then broke the v1.0.0 GitHub release at tick 14 | 3 | 2026-09-22 |
| M-008 | green-worker, final-gate | semantic-release / bot commits with `[skip ci]` can rewrite tracked files (e.g. CHANGELOG.md header) that your tests assert on; re-run the full matrix locally after any automated release commit lands rather than trusting the prior green. | sprint1 S1-06/fix2, TestChangelogSeededHeader broke on master after release commit 4d44590, required S1-06/fix3 | 1 (high blast-radius; kept) | 2026-09-22 |
| M-009 | structural-reviewer | gate-structural-integrity's `_test.go` carve-out only covers .ts/.tsx/.js/.mjs/.rs; Go function-local variables/fields declared in separate test functions (stdout, cfg, buf, body...) are misreported as HIGH foundation-poisoning. Verify the actual scope before treating the finding as real, and report it (per the plugin-issues note) rather than "fixing" working code. | sprint1 S1-01/review, S1-02+S1-04/review, S1-06/review, S1-03+S1-05+S1-07/review | 4 | 2026-09-22 |
| M-010 | structural-reviewer, planner | Cross-package Go test helpers with identical names (repoRoot, readRepoFile, indentOf, topLevelBlock, jobBlock, section helpers) are expected duplication, not poisoning, because packages can't share unexported test code; scope a dedicated test-support consolidation task for it instead of asking a single story to fix it. | sprint1 S1-02+S1-04/review, S1-06/review, S1-03+S1-05+S1-07/review; logged as technical debt for next planning | 3 | 2026-09-22 |
| M-011 | all | `md-db validate` can print `files: []` on a fully valid run; that line is not a failure signal by itself, confirm pass/fail from the actual gate/test exit code. | sprint1 Plugin issues log (reported twice, confirmed by S1-05 planner's negative test) | 2 | 2026-09-22 |
| M-012 | red-worker, green-worker | Keep each task's diff scoped strictly to its owned files (verify with git diff/stat before reporting); this is what let the orchestrator re-verify quickly and fast-forward/merge chains cleanly across every story. | sprint1 task log, universal ("diff = N files only" on nearly every row) | 8+ (every story) | 2026-09-22 |
| M-013 | structural-reviewer | Reviews are budgeted at 5 minutes; two ran over (6m24s, 8m59s) with the second not caught by the orchestrator. Report the verdict as soon as it is reached instead of continuing to poke at an already-identified defect class. | sprint1 S1-01/review, S1-06/review | 2 | 2026-09-22 |
